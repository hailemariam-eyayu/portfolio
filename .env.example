# Portfolio Admin Configuration
# Copy this file to .env.local and update the values

# Admin password for project management (add, edit, delete)
# In production, use a strong password
NEXT_PUBLIC_ADMIN_PASSWORD=your_secure_password_here

# Note: NEXT_PUBLIC_ prefix makes this variable available in the browser
# In a real production app, consider using server-side authentication instead